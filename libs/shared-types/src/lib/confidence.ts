/**
 * Below this, an extracted line item still needs the owner's eyes: the review
 * screen flags it, and confirming a session is refused while one remains.
 */
export const LOW_CONFIDENCE_THRESHOLD = 0.7;
